
  function iniciarTemporizador() {
    // Establecer el tiempo en milisegundos (por ejemplo, 5000 para 5 segundos)
    var tiempo = 5000;

    // Llamar a la función después del tiempo especificado
    setTimeout(function() {
      alert('¡Tiempo terminado!');
    }, tiempo);
  }


